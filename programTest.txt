program myProgram;
    var int: i, j, k;
	var float: a[5], b[5];

func int sum(int x, int y) {
    return x + y;
}

func int mult(int x, int y) {
    return x * y;
}



main() {
    i = 10;
    j = 15;

    k = sum(mult(i, j), j);

    write("k = ", k, "\n");

	end;
}