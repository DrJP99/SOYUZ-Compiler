program myProgram;
	var int: a, b, c;

func int mul(int a, int b) {
    return a * b;
}

func int myFunc(int x, int y) {
	var int: z;
    write("x = ", x, ", y = ",  y, "\n");
	z = x + y;
    write("z = ", z, "\n");
	return z;
}

main() {
    var int: h, g, i, j, k;

    read(h);
    read(g);

	a = myFunc(h, g);
    write(h, " + ", g, " = ", a, "\n");
    
    read(i);
    read(j);
    k = mul(i, j);
    write(i, " * ", j, " = ", k, "\n");

	end;
}